# ForumMind 产品架构

ForumMind 是一个面向科研课题组的通用 AI Scientist。产品以课题组工作台为统一入口，把研究问题、资料、Agent 协作、实验数据、组会讨论、PI 决策和科研 Memory 组织在同一条“问题—计划—执行—反馈”链路中。

架构的核心目标不是让 AI 直接给出一个结论，而是让每个判断都能回到来源、假设、反例、实验结果和决策过程。AI 负责整理、质询、分析和提出候选方案，研究者负责审阅证据、执行真实实验并作出最终科研决策。

## 1. 架构原则

- **一个课题，一个连续上下文。** 课题组是成员、对话、资料、任务、运行、产物和 Memory 的共同边界。
- **证据先于结论。** Agent 输出先成为带来源和不确定性的候选结果，经过校验和用户审阅后才进入正式研究记录。
- **问题驱动协作。** 模糊想法先通过任务澄清变成目标、约束、资料范围和交付形式，再进入多 Agent 分析。
- **人保留决策权。** PI 对研究路线、实验投入、阶段性结论和正式 Claim 拥有最终决定权。
- **研究历史可回溯。** Memory、实验数据、候选结果和会议事件采用版本化或追加式记录，不静默覆盖过去的判断。
- **数据空间显式隔离。** 合成数据、真实数据、脱敏真实数据和可验证公开来源保持清晰区分，运行、工具、资料和结果必须属于一致的数据空间。

## 2. 总体架构

    研究者
      |
      v
    Next.js Web（apps/web）
      |  课题组工作台、聊天、资料、任务、运行、审阅、Memory
      v
    FastAPI API（apps/api）
      |-- 身份与课题组边界
      |-- Agent、任务、资料、实验、组会和产物服务
      |-- 科研编排、Run 状态机、候选结果与 PI 决策
      |-- 来源校验、工具授权、审计和持久化
      |
      |  内部鉴权桥接
      v
    Native Pi Runtime（apps/pi-runtime）
      |-- Agent Session 管理
      |-- 受控工具入口
      |-- Runtime Event 日志与游标恢复
      +-- 结构化结果和运行状态回传
      |
      v
    SQLite + 受控文件存储
      |-- 课题、Agent、消息、任务、Run、会议、Memory 和审阅记录
      |-- 文档元数据、分块、索引、来源和实验数据版本
      +-- 产物文件、上传文件和实验源文件

### 分层职责

| 层 | 主要职责 | 不负责的事情 |
| --- | --- | --- |
| Web | 提供课题组工作台、聊天时间轴、资料入口、任务澄清、运行进度、候选审阅和 Memory 视图 | 不决定服务端事实，不直接访问数据库或模型密钥 |
| API | 维护产品领域对象、权限范围、科研流程、来源校验、状态变更和持久化 | 不把未经验证的模型文本直接升级为正式结论 |
| 科研编排 | 将任务拆成 Agent 调用、资料检索、实验分析、辩论、组会和决策步骤 | 不绕过数据空间和审查门 |
| Pi Runtime | 执行单个 Agent Session，接收受控上下文，调用被授权工具并回传事件 | 不拥有科研流程、正式 Memory、权限或 PI 决策主权 |
| 持久化 | 保存可恢复的对象、事件、版本、来源和文件 | 不以浏览器缓存作为业务事实来源 |

## 3. 课题工作空间

课题组工作台是产品的主交互空间，采用“顶栏 + 聊天时间轴 + 输入器”的连续布局。成员、资料、任务、会议和产物通过用户主动打开的面板查看，研究过程始终保持在同一个课题上下文中。

### 核心对象

| 对象 | 作用 | 关键关联 |
| --- | --- | --- |
| 课题组 | 组织研究上下文和数据空间 | 成员、消息、任务、资料、Run、会议、Memory |
| Agent Profile | 描述成员角色、能力、工具和资料权限 | 课题组、任务、Agent Session |
| 研究问题 | 定义要回答的科学问题及约束 | 任务、假设、证据、实验、决策 |
| 任务 | 将研究问题转成可执行范围 | 澄清记录、授权资料、数据集、Run |
| 文档与来源 | 保存论文、记录和其他研究材料 | 文档分块、来源位置、任务授权、候选引用 |
| 证据 | 表示可定位的文献、文件、实验或人工记录 | 候选 Claim、正式 Claim、Memory |
| Candidate Claim | 待审阅的结构化候选判断 | Run、证据引用、批准或拒绝记录 |
| Formal Claim | 经过用户批准的正式阶段性判断 | 证据、Research Memory、会议和后续任务 |
| 实验数据集 | 保存实验表格的不可变版本 | 样品、字段、分析、源行列、任务 |
| Run | 表示一次受控科研分析及其事件流 | Agent 调用、工具审计、候选、产物、会议 |
| 会议事件 | 记录 Agent 汇报、PI 插话和 PI 决策 | Run、产物、行动项、Research Memory |
| Research Memory | 追加保存问题、假设、证据、实验、争点和决策的变化 | 时间线、版本树、证据图谱、下一轮任务 |
| 产物 | 保存研究报告、会议材料和其他可查看结果 | Run、Agent、来源、版本和会议 |

### Agent 角色

Agent 以不同的研究职责参与同一个问题。常见角色包括：

- **博士后角色：** 负责把模糊现象澄清成可执行任务，识别实验约束并提出阶段性研究方向。
- **博士角色：** 负责拆解问题、审查产物、比较解释并组织阶段性结论。
- **硕士或专项角色：** 从文献、机理、实验方法、数据分析等角度独立提出证据和反例。

角色差异由 Agent Profile 的能力、可用工具、允许的数据空间和输出契约共同约束。多个 Agent 可以独立分析同一问题，但结果必须回到同一个 Run 和同一组来源边界中进行比较。

## 4. 问题—计划—执行—反馈链路

    研究问题
        |
        v
    任务澄清：目标、约束、资料范围、交付形式
        |
        v
    研究计划：竞争性假设、预测、反例、判别实验
        |
        v
    受控执行：资料检索、文献连接、实验数据分析、Agent 辩论
        |
        v
    候选结果：证据、引用、适用边界、不确定性
        |
        v
    组会审查：攻击、回应、未决分歧和 PI 决策
        |
        v
    真实实验与结果导入
        |
        v
    Research Memory：更新假设、证据、结论和下一轮问题

### 4.1 问题与任务澄清

用户可以在课题组聊天中通过 @ 指定一个 Agent 或全体成员，描述现象、已有观察和希望得到的帮助。Agent 逐轮确认研究对象、变量、约束条件、资料范围和交付格式。澄清记录会持久化到课题组上下文；任务确认后，服务端创建或复用受范围约束的研究任务。

### 4.2 计划与多 Agent 分析

研究任务冻结本次 Run 使用的课题组、Agent、资料范围、数据空间和任务约束。编排层根据 Agent Profile 生成受控调用，组织独立观点、攻击与回应、证据审查和判别实验建议。Agent 的自然语言输出只是一种过程材料，不能跳过来源校验和候选审阅。

### 4.3 执行与候选结果

服务端为每次 Run 记录输入范围、工具调用、运行事件、产物和结果校验状态。工具结果应包含数据空间、来源引用和必要的定位信息。候选 Claim 只有在结构、引用、Run 归属和权限范围均通过校验后，才会进入待审阅状态。

### 4.4 反馈与 Memory

用户可以批准或拒绝候选 Claim。批准会生成正式 Claim 及其证据关联，拒绝会保留候选和审阅记录，但不会生成正式 Claim。组会中的 PI 插话、决策和行动项会与 Run 和产物关联，并回写到追加式 Research Memory，形成下一轮研究任务的上下文。

## 5. Web 与 API 边界

### Web 工作台

Web 层负责将服务端对象投影为可理解的研究体验：

- 课题组创建、成员配置和课题上下文展示；
- 持久化聊天、附件、任务澄清和会议安排；
- Live Run 的实时进度、成员消息、产物和终态；
- 文档来源、实验数据集、候选 Claim 和审阅操作；
- Memory 的时间线、版本树和证据图谱视图。

浏览器通过 API 访问业务数据。localStorage 只保存成功响应的缓存、最近的课题线索和 run_id + event_cursor 恢复线索；首页和工作台必须先读取服务端。缓存过期或服务端读取失败时，界面应提示信息未确认，而不是把缓存当成当前事实。

### API 领域边界

| API 领域 | 主要职责 |
| --- | --- |
| 课题组与消息 | 创建课题组、管理成员、保存聊天消息、任务澄清和会议安排 |
| Agent | 管理 Agent Profile、能力、工具范围和测试状态 |
| 文档与知识 | 上传、解析、分块、索引、受授权检索和来源定位 |
| 研究任务 | 创建任务、绑定文档和实验数据版本、冻结任务范围 |
| Run 与会议 | 启动和恢复运行、提供事件流、保存 Agent 汇报、PI 插话和决策 |
| 候选与研究记录 | 返回候选 Claim、执行批准或拒绝、生成 Formal Claim 和证据关联 |
| 实验 | 预览和导入 CSV/XLSX、保存不可变版本、执行受控分析和返回 provenance |
| 产物与 Memory | 保存报告、会议材料、版本关系、研究状态和只读投影视图 |
| Runtime Bridge | 向 Pi Runtime 发送受控 Session 请求，接收事件和结构化结果 |

API 是所有正式状态变化的入口。浏览器不能直接写 SQLite、正式 Memory、Candidate、Formal Claim 或运行时事件表。

## 6. Agent Runtime 与工具治理

### API 控制面与 Pi 执行面

ForumMind 将科研流程控制与单 Agent 执行分开：

    API 控制面
      读取任务范围和 Agent Profile
      检查成员、工具、资料和数据空间权限
      创建 AgentInvocation
      持久化 Run、Session ownership 和事件游标
      校验结果并生成候选对象
              |
              v
    Pi 执行面
      创建或恢复单个 Native Pi Session
      接收结构化指令和受控上下文
      调用 API 提供的 governed tools
      记录事件并回传文本、工具结果和结束状态

Pi Runtime 只管理 Session、事件日志、工具调用协议、并发和资源限制。它不拥有课题组流程、权限策略、正式 Memory、文档存储、候选审阅或 PI 决策。

### 受控工具

工具由服务端 Registry 和策略层共同授权。工具结果默认是候选上下文，不直接推进科研阶段、不写正式 Memory，也不替代 PI 决策。

| 数据空间 | 允许的工具能力 |
| --- | --- |
| synthetic | 受控 Memory 查询、合成文献检索、合成实验分析 |
| real / desensitized_real | 课题授权资料检索、实验数据分析、可验证公开文献检索 |
| verifiable_public | 公开来源元数据、来源可达性和定位校验 |

每次调用都会绑定课题组、任务、Agent、Run、数据空间和来源范围。请求或结果发生数据空间不一致、来源越界、契约不完整或角色不允许使用工具时，运行进入可解释的失败状态。

### 运行状态与恢复

Run 的终态、快照、Runtime Event、Session ownership 和事件游标由服务端持久化。Web 优先通过 SSE 接收增量事件，并以快照补齐断线期间的内容；恢复时使用 run_id + event_cursor，不会重复产生业务结果。

服务端在调用 Runtime 前先记录运行启动事件，运行失败时保留失败状态和原因。系统不能用合成 Scenario 文本或旧缓存伪装成一次成功的 Live 运行。

## 7. 资料、实验与证据链

### 文档与知识检索

上传的 PDF、DOCX、TXT 和 Markdown 文件进入受控文件根目录，SQLite 保存文档元数据、哈希、存储键、数据空间、解析状态和分块信息。文档解析后建立全文索引，并结合本地确定性向量表示进行混合检索。

检索结果至少需要携带文档标识、分块标识、位置和数据空间。任务只可以检索已授权且状态可用的资料，Agent 不能自行决定文件名、存储路径或扩展访问范围。

### 实验数据

实验工作区接收 CSV/XLSX 文件，导入时校验样品标识、字段类型、单位、数值和缺失值。每次确认导入都会创建新的不可变数据集版本，源文件和源行列关系保留在 provenance 中。

系统提供摘要、相关性和分组均值等受控分析。分析结果必须关联具体数据集版本和源行列，并明确相关性不自动代表因果关系。真实实验、样品制备、设备操作、测试表征和结果解释由研究者负责。

### 候选 Claim 与 Formal Claim

    来源资料 / 实验数据
              |
              v
    类型化 Evidence + source links
              |
              v
    Candidate Claim
      引用、范围、结构、数据空间和 Run 归属校验
              |
              +-- approve --> Formal Claim + claim approval + Research Memory
              |
              +-- reject  --> 保留候选、证据和审阅理由，不生成 Formal Claim

候选内容可以包含判断、依据、适用边界、反例、预测、不确定性和可推翻条件。正式 Claim 只表示“研究者批准将该判断纳入课题记录”，不表示系统已经完成科学验证。

## 8. 持久化与事实来源

### SQLite

SQLite 是本地部署中的服务端事实来源，保存：

- 课题组、用户、成员关系、Agent Profile 和 Agent 测试结果；
- 聊天消息、任务澄清、研究任务和会议安排；
- Run 快照、产物元数据、Runtime Session、Runtime Event 和游标；
- 文档、文档分块、索引任务、任务资料关系和来源记录；
- 实验数据集版本、分析记录、源文件关系和数据 provenance；
- Candidate Claim、Evidence、审批记录、Formal Claim 和研究状态版本；
- 工具调用审计、公开文献 lead 和跨对象的来源关联。

### 受控文件存储

文件内容由服务端生成存储键并写入受控根目录。数据库保存文件名、哈希、数据空间、来源和版本元数据，用户和 Agent 不能通过输入路径直接读写任意文件。

默认路径和配置边界如下：

| 配置 | 作用 |
| --- | --- |
| FORUMMIND_DB_PATH | SQLite 数据库位置，默认使用 apps/api/data/forummind.db |
| FORUMMIND_STORAGE_ROOT | 文档和产物的受控文件根目录 |
| FORUMMIND_EXPERIMENT_SOURCE_ROOT | 实验源文件保留目录 |
| CORS_ORIGINS | API 接受的 Web 来源 |
| NEXT_PUBLIC_API_BASE_URL | Web 访问 API 的地址 |

### 追加式研究 Memory

Memory 的原始记录以追加方式保存，每个条目可关联研究对象、来源、Run、实验版本或会议事件。系统在原始记录之上提供时间线、版本树和证据图谱三个只读视图，并可从 Run 的步骤和 Memory 条目投影实验视图。投影不修改原始 Memory，也不改变历史版本。

## 9. 权限、可信等级与失败边界

### 权限范围

权限由用户身份、课题组成员关系、Agent Profile、研究任务和数据空间共同决定。一次 Run 会冻结实际使用的任务范围；运行中新增的文件、数据集或工具不会自动获得访问权限。

内部 Pi Bridge 使用独立运行时令牌。模型 API Key 只由后端读取和使用，Web 不返回密钥，也不将密钥写入聊天、产物或研究记录。

### 数据可信等级

- **Synthetic：** 用于合成案例、回归和可控演示，不应被误认为真实实验事实。
- **Real：** 真实科研资料或实验数据，只能在明确授权的课题和任务中使用。
- **Desensitized real：** 已脱敏的真实资料，仍需遵守课题和 Agent 的授权范围。
- **Verifiable public：** 可访问并能定位的公开来源，用于补充公开文献证据。

数据空间会贯穿课题组、任务、Agent、文档、实验数据、工具请求、工具结果和候选引用。任何一处不一致都会阻止结果进入正式证据链。

### 产品可信边界

- AI 输出是可审查的资料整理、候选解释、质询和实验建议，不是自动生成的最终科学结论。
- 无法定位到来源、引用越界或无法通过结构校验的内容不能成为正式证据。
- 相关性、模型推断和单次观察不会自动升级为因果结论。
- AI 不执行真实实验、不控制设备、不修改原始实验文件，也不替 PI 决定研究路线。
- 失败运行、被拒绝候选、反例和退回理由都会保留，以便回溯研究过程。

## 10. 部署形态

本地产品由三个服务组成：

    Web        http://localhost:3000  Next.js
    API        http://localhost:8000  FastAPI
    Pi Runtime 内部地址              Node Native Pi sidecar

Web 只通过 API 访问科研对象。Pi Runtime 只在内网中暴露给 API，通过受控令牌和 Session 根目录运行。Docker Compose 将 Web、API 和 Pi Runtime 编排为三个服务，并为 Pi Session 使用受控 named volume。

外部模型服务通过 OpenAI-compatible 接口接入，配置保留在后端运行环境。公开文献通过服务端适配器检索和保存 lead/provenance；文档、实验数据和模型调用不会要求浏览器直接连接外部服务。

## 11. 架构要点

ForumMind 的系统边界可以归纳为四条规则：

1. **研究上下文由课题组统一承载。** 聊天、资料、任务、运行、会议和 Memory 彼此关联，用户不需要重复搬运背景信息。
2. **科研流程由 API 控制。** 任务范围、工具权限、来源校验、候选审阅和 PI 决策都由服务端决定。
3. **Agent Runtime 只负责执行。** Pi 提供可恢复的单 Agent Session 和受控工具调用，不拥有正式科研状态。
4. **每个判断都有回溯路径。** 从 Formal Claim 可以回到审批、Candidate、Evidence、来源位置、Run、实验版本和相关会议事件。

因此，ForumMind 不是一个只返回答案的聊天机器人，而是一个把问题、计划、执行和反馈组织成可审查科研过程的 AI 课题组。
